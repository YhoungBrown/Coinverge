import { StyleSheet } from "react-native";

const styles =  StyleSheet.create({
    container: {
    padding: 1,
    borderRadius: 12,
  },
  title: {
    marginTop: 7,
    textAlign: "center",
    fontSize: 11,
    fontWeight: "bold",
    marginBottom: 1.5,
  },
})

export default styles;